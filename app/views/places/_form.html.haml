= simple_form_for place, remote: true, defaults: { required: false } do |f|
  = f.error_notification
  .form-inputs
    = f.input :name
    = f.input :description
    = f.input :image, placeholder: "http://"
  .form-actions
  = f.button :submit
